version: "3"

services:
  web:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: next_app
    ports:
      - "3000:3000"
    volumes:
      - .:/app
      - /app/node_modules
    command: npm run dev
